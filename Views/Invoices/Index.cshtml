@model IEnumerable<Restaurant.Models.Invoices>

@{
    ViewBag.Title = "Index";
}

<h3 class="alert alert-info text-center">Invoice List</h3>

<div class="d-flex flex-row-reverse mb-3">
    @*    @Html.ActionLink("Create Invoice", "Create", null, new { @class = "btn btn-primary btn-lg" })
    *@
</div>
<table class="table table-striped table-hover text-center">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InvID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InvoiceTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tips)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PayMethods.Method)
        </th>
        @*        <th>Actions</th>
        *@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            
                <td>
                    @Html.DisplayFor(modelItem => item.InvID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.InvoiceTime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Total)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tips)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PayMethods.Method)
                </td>
                @*<td>
                @Html.ActionLink("Edit", "Edit", new { id = item.InvID }) |
                @Html.ActionLink("Details", "Details", new { id = item.InvID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.InvID })
            </td>*@
            </tr>
    }

</table>
