@model IEnumerable<Restaurant.Models.InvoiceDetails>

@{
    ViewBag.Title = "Index";
}

<h3 class="alert alert-info text-center">Invoice Details</h3>

<div class="d-flex flex-row-reverse mb-3">
    @*    @Html.ActionLink("Create Invoice", "Create", null, new { @class = "btn btn-primary btn-lg" })
    *@
</div>
<table class="table table-striped table-hover text-center">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Invoices.InvID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SubTotal)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Orders.OrderID)
        </th>
        @*        <th>Actions</th>
        *@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Invoices.InvID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubTotal)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Orders.OrderID)
            </td>
            @*<td>
            @Html.ActionLink("Edit", "Edit", new { id = item.SSN }) |
            @Html.ActionLink("Details", "Details", new { id = item.SSN }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.SSN })
        </td>*@
        </tr>
    }

</table>
