@model IEnumerable<Restaurant.Models.Orders>

@{
    ViewBag.Title = "Index";
}

<h3 class="alert alert-info text-center">Order List</h3>

<div class="d-flex flex-row-reverse mb-3">
@*    @Html.ActionLink("Create Order", "Create", null, new { @class = "btn btn-primary btn-lg" })
*@</div>
<table class="table table-striped table-hover text-center">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrderID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TableTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AllPaid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AllPaidTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tables.TbID)
        </th>
@*        <th>Actions</th>
*@    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TableTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AllPaid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AllPaidTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tables.TbID)
            </td>
            @* <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.OrderID }) |
            @Html.ActionLink("Details", "Details", new { id = item.OrderID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.OrderID })
        </td>*@
        </tr>
    }

</table>
